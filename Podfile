# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'fndb-ios' do
  pod 'Firebase'
  pod 'Firebase/Database'

  use_frameworks!

  # Pods for fndb-ios

end
